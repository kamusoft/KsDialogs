# Deviation: adopt-docs-refresh

- tasks 2.2「『platform / Sample ディレクトリに README を置かない』の根拠参照 (cross/ADR-0023) を KsDialogs の phase-2 踏襲決定への参照に直す」: tasks では翻案元の禁止規範を根拠差し替えで残す → 実装では禁止規範を書かず、追従対象の範囲だけを述べる (`.agents/skills/docs-refresh/SKILL.md:41`「samples/ 配下の開発者向け README は利用者向けでないため `readmes` に載せず追従対象にしない。追従対象の増減は変更フローが `readmes` を書き換えたときのみ」)。理由: 根拠となる決定が KsDialogs に存在せず (phase-2 は未議論)、`samples/README.md` 等 6 本が実在して `kasane/config.yaml` の `ui.screenshot` が正典として参照している現役文書のため、指示どおりでは未成立の決定を根拠に現状と矛盾する規範を書くことになる。デルタスペックの Requirement「追従対象の規範」は README 4 枚と manifest の `readmes` が正であることのみを求めており仕様違反ではない (review-001 Suggestion も同判断)。オーケストレーターの既定 A として記録、オーナー確認待ち (2026-09-04)
- Requirement「規約記述と lint 範囲」(`lint.identity.scope` に `skills` を含む): spec では `kasane` + `skills` → 指示によりルート `README.md` / `README_ja.md` も scope に追加 (docs-refresh の更新対象であるルート README を identity-lint の範囲に入れる。review-002 / second-opinion-code-001 で降格していた指摘をオーナーが今対応と判断)。理由: 公開成果物の保護 (2026-09-04)
- Requirement「docs-refresh 一式の配置と起動経路」(`scripts/` 8 本は翻案元と同一内容・無改変): spec では byte 一致 → 指示により `/tmp/docs-refresh-*` の固定パスを KsDialogs 固有 (`/tmp/docs-refresh-ksdialogs-*`) に分け、直書きしていた `scripts/link-resolution-check.py` は環境変数 `DOCS_REFRESH_TARGETS` (既定は翻案元と同じ `/tmp/docs-refresh-targets.txt`) で受けるよう改変。理由: 姉妹リポジトリ KsSettingsView の docs-refresh と `/tmp` の予定 manifest / 対象一覧が衝突する (review-002 Suggestion。現に KsSettingsView 側の残骸を確認)。以後 8 本のうち link-resolution-check.py だけが翻案元と差分を持つ (2026-09-04)
- [付随修正] `.gitignore` を KsSettingsView の構成を参考に整備 (一般・IDE・AI 関連・機密・Kasane 証跡の例外)。spec の対象外だが、`.agents/` 一式の追跡方針 (KsSettingsView と同じく追跡する) を確認する流れでオーナーが指示 (2026-09-04)
