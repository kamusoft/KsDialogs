# Deviations: rollout-user-docs

- Requirement「移行 Skill の対応表」/ Scenario「Toast 節の内容」: spec では旧 `Toast.Instance.Show(message)` を新 Toast の message 入口へ対応付ける → 指示により、移植元に実在する旧 `Show<TView>(object viewModel = null)` と具象 View 経路は「対応先なし」とし、新 Toast の message 入口は移行後に新規採用できる代替として挙動差 3 点とともに案内する。理由: 指定された移植元 clone の README・公開ソース・全 Git refs と履歴に旧 message overload が存在しないため (2026-09-05)
- Requirement「移行 Skill の対応表」/ Scenario「旧公開 API の網羅」: 移植元 README では旧 `IReusableLoading.Hide()` を `void Hide()` と記載する → 指示により、公開 interface と iOS / Android 実装に一致する `Task Hide()` を旧署名として掲載し、README の記載差を注記する。理由: 移行者が実際にコンパイルする公開 API を優先するため (2026-09-05)
- Task 3.2「スクリーンショットの提示」: spec では Sample のデモ駆動モードで候補を撮影する → iOS Toast は通常 UI の表示完了後に実メニューの `Toast Stack` を 1 回操作し、同じ表示経路の完全表示フレームを撮影した。理由: launch argument は届くものの表示ホスト準備前に自動再生が取りこぼされ、消えかけでない 3 枚表示を安定取得できなかったため。ソース・duration・表示経路は変更していない (2026-09-05)
- Task 5.4「リポジトリ全体の lint」: tasks では `doc-structure-lint.py` をリポジトリ全体で通す → 今回変更した長命文書の違反 0 件を合格条件とし、既存の長命文書・過去ロードマップ 42 ファイルに残る 261 件は継承 baseline として報告する。理由: 既存文書の全構造改修は本 change の利用者向けドキュメント展開を超え、凍結済み・過去記録も含むため (2026-09-05)
- [付随修正] `.agents/skills/docs-refresh/SKILL.md` と `references/prompt-readme.md`: リポジトリ内部用語と ADR の禁止を利用者向け Skill に限定し、ルート README では repository-docs spec が必須とする `kasane/` 行と concepts 導線を許可した。理由: README の必須構成を閉世界検査が誤検出する契約衝突を解消するため (2026-09-05)
- [付随修正] `kasane/concepts/core/api/toast-semantics.md`: 既定エントリ表の Android / MAUI / KMP を、現行実装で確認した `Toast.instance` / `Toast.Instance` / `Toast.instance` へ事実訂正し、timestamp と manifest hash を更新した。理由: Skill 生成時の源泉照合で、旧表が型名だけを記して singleton の実名と一致していないことを発見したため。公開契約の変更はない (2026-09-05)
- [付随修正] cross/ADR-0006・0007: accepted 本文と過去の現行照合は不変の履歴として保持し、2026-09-05 の現行照合 footer だけを追記して、参照方式は `local-development-setup.md`、パリティ規約は `sample-parity.md` が現在の所在であることを示した。理由: task 7.2 の当初列挙から漏れていた履歴上の参照をクロスモデル最終レビューで発見したため。Sample の配置・consumer 境界・パリティの決定内容は変えない (2026-09-05)
- Requirement「廃止した README への参照の解消」/ Task 7.4: 承認済み Scenario は `kasane/changes/archive/` とロードマップ過去記録しか除外しないが、proposal / design など進行中 change の足場、append-only の `kasane/concepts/log.md` と `kasane/lessons/inbox/`、accepted ADR-0006・0007 の既存本文・過去 footer も移送元のパスを履歴として保持するため、全文字列 0 件は達成不能だった。足場と accepted 履歴は書き換えず、これらの履歴・作業記録を除外した active な decisions・handbook・concepts・実装・公開文書で残存参照 0 件を合格条件とする。`kasane/lessons/<scope>.md` の昇格済み規律は除外しない (2026-09-05)
- Task 9.1「全ビルドルートの全件実行」: 本 change は製品コード・テストを変更しないため、proposal Impact の合意済み例外に従い全ビルドルートの全 suite は省略した。変更範囲に対応する docs-refresh 検査・公開 API の限定 consumer build・関連テストは各レビューで実行した。なお KMP の限定 metadata compile で発見した既存不具合は成功扱いせず、`fix-kmp-iosmain-throws-metadata` に簡易起票し、当時の最新 `review-021.md` は CHANGES_REQUESTED を維持した。同 change の修正取り込み後、KMP library の iosMain metadata compile と KMP Sample の commonMain metadata compile の成功を `review-024.md` で確認し APPROVED へ更新済み (2026-09-05)
