# 3.1 4 形態の dry-run (引数なし) (2026-09-09)

判定: **4 形態すべて成功**

作成物: `verification/{ios,android,maui,kmp}/prepare-feed.sh` と `build-consumer.sh`、
共有ライブラリ `verification/lib/gradle-publish.sh` (ローカル Maven リポジトリへの発行と
本体側の合成 Swift マニフェストの復元)・`verification/lib/render-template.py`
(`@KSV_DEPENDENCY@` の置換)、`verification/lib/verification-args.sh` に
`ksv_prepare_work` / `ksv_reset_dir` を追加。

作業ディレクトリはスクラッチ配下 (`<scratch>/work/ksdialogs-verification/<形態>`)。
`--work` の指定が無いときの既定は `${TMPDIR:-/tmp}/ksdialogs-verification/<形態>` で、
リポジトリ内は使わない (`ksv_prepare_work` が拒む)。

## 実行と所要時間 (手元。Gradle デーモンと NuGet キャッシュは温まった状態)

  $ verification/ios/prepare-feed.sh          成功   (同期のみ)
  $ verification/ios/build-consumer.sh        成功   14 秒 (フィード準備込み)

  $ verification/android/prepare-feed.sh      成功   3 秒
  $ verification/android/build-consumer.sh    成功   4 秒 (フィード準備込み。2 module の
                                                     assembleRelease と依存ツリー 2 本)

  $ verification/maui/prepare-feed.sh         成功   19 秒 (pack 3 件)
  $ verification/maui/build-consumer.sh       成功   3 分 26 秒 (フィード準備込み。
                                                     restore + Android Release + iOS Release)

  $ verification/kmp/prepare-feed.sh          成功   23 秒 (clone の tag + android/ と kmp/ の発行)
  $ verification/kmp/build-consumer.sh        成功   48 秒 (フィード準備込み。3 段 + 依存検査)

いずれも最終行に参照先を出力する。

  ios      <scratch>/work/ksdialogs-verification/ios/KsDialogs-SPM
  android  <scratch>/work/ksdialogs-verification/android/maven
  maui     <scratch>/work/ksdialogs-verification/maui/feed
  kmp      <scratch>/work/ksdialogs-verification/kmp/maven

KMP の `prepare-feed.sh` は参照先の 1 行前に Swift 参照の URL
(`file://<scratch>/work/ksdialogs-verification/kmp/KsDialogs-SPM`) を出す。

## Android SDK の解決

`ANDROID_HOME` / `ANDROID_SDK_ROOT` を未設定にした環境で実行し、Android と KMP の両方が
本体 build root の設定から解決した。

  ANDROID_HOME を <repo>/android/local.properties の sdk.dir から解決しました

解決はフィード準備 (本体の発行) の前に行う。

## KMP の 3 段

  ==== 1 段目: Android アプリの Release ビルド ====      BUILD SUCCESSFUL in 5s
  ==== 2 段目: 合成 package の再生成と共有モジュールの framework リンク ====
      :shared:integrateLinkagePackage                  BUILD SUCCESSFUL in 597ms
      :shared:linkReleaseFrameworkIosSimulatorArm64     BUILD SUCCESSFUL in 18s
  ==== 3 段目: iOS アプリの Release ビルド ====           ** BUILD SUCCEEDED **

2 段目の合成 package の再生成は `XCODEPROJ_PATH` を与えた `:shared:integrateLinkagePackage`
で行う (リンクのタスクは再生成しない)。

## 追跡物に差分が出ないこと

4 形態の dry-run をすべて通した後の状態。

  $ git status --porcelain
   M kasane/changes/add-consumer-verification/tasks.md
  ?? kasane/changes/add-consumer-verification/deviation.md
  ?? kasane/changes/add-consumer-verification/evidence/
  ?? scripts/readme-example-lint.py
  ?? verification/

`kmp/.swiftpm-locks/` に差分は無い (KMP のフィード準備が kmp/ の発行の後に合成 Swift
マニフェスト 2 本を `git checkout --` で戻し、「本体側の合成 Swift マニフェスト 2 本を
追跡状態へ戻した」と出力する)。`verification/kmp/` の追跡予定の生成物 2 本も smoke 形の
まま変わらない。

  $ grep url: verification/kmp/iosApp/VerificationApp/Package.swift \
              verification/kmp/iosApp/KotlinMultiplatformLinkedPackage/subpackages/*/Package.swift
  ... url: "https://github.com/kamusoft/KsDialogs-SPM",   (2 本とも)

## 削除の扱い

作業ディレクトリ直下の使い捨てディレクトリ (`maven` / `feed` / `packages` / `consumer` /
`trees`) は `ksv_reset_dir` が実行ごとに作り直す。削除の前に、作業ディレクトリが絶対パスの
実在するディレクトリであること・名前が単一階層であること・対象がリポジトリの中でないことを
全件検証する。MAUI の `build-consumer.sh` は消費者プロジェクト直下に残った過去のビルド出力
(`obj` / `bin`) も取り除く (下記)。
