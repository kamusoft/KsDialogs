---
scope: spec-review
kind: pain
severity: normal
count: 2
first-seen: 2026-08-22
last-seen: 2026-08-25
evidence:
  - add-presentation-behavior (ui/brief.md の文言表が Transition デモ画面の表示操作を `表示` (日本語) と定めてモック承認まで通ったが、先行 change で確定済みの Layout Dialog パネルの同じ役割の操作は `Show` (英語) で、同じ Sample 内の画面間で表記がそろわない状態で4ルート実装が完了。実装フェーズ末に気づき、統一するか画面ごとの差を許容するかがオーナー判断 (蒸留時) へ持ち越された)
  - add-model-binding-di (ui/brief.md の「画面と状態」が Model Dialog のメニュー行を遷移記号 `›` なしと定めたが、既存メニュー行はすべて `›` ありで sample-parity 規約も4ルート一致で `›` を課しており、同じ文の「既存行と同じ行デザイン」と矛盾。実装は既存準拠の `›` ありを採り、オーナー裁定 2026-08-25 で承認 — brief 起こし時に既存画面の同じ役割の表記を引いていれば食い違い自体が生じなかった)
---

## ルール文

ui/brief.md の文言表を新画面向けに起こすとき、既存画面の文言表 (handbook/cross/sample-parity.md の「文言の正」と各パネルの表) から同じ役割の項目 (表示操作・完了/キャンセル・戻る・結果見出し) を引き、同じ役割には同じ表記を採る。意図して別表記にする場合は brief に「既存の `<文言>` と違える理由」を1行書いてからモック承認へ出す。

## 経緯

- 2026-08-22 add-presentation-behavior: brief の文言表は単体では一貫していたが、既存画面との突き合わせを経ずに承認ゲートを通った。Sample は4ルートの横並びだけでなく画面間の一貫性も検証装置としての前提であり、表記の揺れは後から S 級の UI 微調整 change を要する
- 2026-08-25 add-model-binding-di: 対象が画面の文言ではなく遷移記号 `›` でも同じ型 — brief が既存メニュー行の表記を引かずに新項目の行を定義し、実装フェーズでの食い違い発見とオーナー裁定 (deviation 記録) を要した
